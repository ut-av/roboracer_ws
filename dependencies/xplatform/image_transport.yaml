name: image_transport
version: 1.0.0
ros_versions:
  - humble
  - jazzy
system:
  - >-
    apt-get update && apt-get install -y ros-$ROS_DISTRO-image-transport
    && rm -rf /var/lib/apt/lists/*
user: []
